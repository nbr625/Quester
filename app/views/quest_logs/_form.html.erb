<%= form_for(@quest_log) do |f| %>
  <% if @quest_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quest_log.errors.count, "error") %> prohibited this quest_log from being saved:</h2>

      <ul>
      <% @quest_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
